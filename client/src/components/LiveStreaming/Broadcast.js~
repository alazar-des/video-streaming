import React, { useEffect, useRef } from 'react';

function Broadcast() {
    const streamVideo = useRef();

    const config = {
	iceServers: [
	    {
		urls: "stun:stun.l.google.com:19302"
	    }
	]
    }

    useEffect(() => {
	
    }, []);
